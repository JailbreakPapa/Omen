OmenNet.Dashboard

Quickstart:

- Build and run the dashboard: `dotnet run --project src/OmenNet.Dashboard`
- Configure dashboard to point at the coordinator via the `appsettings.json` `CoordinatorHost`/`CoordinatorPort` settings.

Add screenshots to `examples/docs-assets/` and link them from `docs-site/docs/dashboard/index.md`.